## Firebase Hosting setup for Vite React SPA (21:13)

**Status**: ✅ Hosting initialized at app root with SPA rewrites; config committed; deploy flow clarified (emulator optional)

**Context**: Set up Firebase Hosting for the frontend at `ea-ai-main2/ea-ai-main2` (Vite + React). Confirmed init belongs at project root (not `src/`). Clarified when to use local emulator vs `firebase deploy`.

**Changes Made**:
- `ea-ai-main2/ea-ai-main2/firebase.json:1-14` — Public set to `dist`; SPA rewrites to `/index.html`; ignore rules for dotfiles/node_modules.
- `ea-ai-main2/ea-ai-main2/.firebaserc:1-5` — Project alias set to `miller-ea-ai`.
- `updates/2025-10-15_devlog.md:1-20` — Added session entry documenting decisions and workflow.

**Decisions**:
- Do not initialize Firebase in `src/`; keep `firebase.json` and `.firebaserc` at app root.
- Keep `dist/index.html` generated by Vite; do not overwrite during `firebase init`.
- Emulator (`firebase emulators:start --only hosting`) is optional for local checks; use `firebase deploy` for live hosting.

**Deploy Workflow**:
1) Build: `npm run build`
2) Deploy: `firebase deploy` (or add script: `"deploy": "npm run build && firebase deploy"`)
3) Live URL printed by CLI (e.g., `https://<site>.web.app`). For preview links: `firebase hosting:channel:deploy <name>`.

**Result**: Frontend is ready to deploy on Firebase Hosting with correct SPA routing and no local file overwrites. Next step: run `npm run build && firebase deploy`.

 
